/**
 * @format
 */

import { AppRegistry } from "react-native";
import HybridApp from "./src/HybridApp";
import { name as appName } from "./app.json";

AppRegistry.registerComponent(appName, () => HybridApp);
